version: "3"

tasks:
  up:
    cmds:
      - docker compose up -d --build {{.CLI_ARGS}}

  down:
    cmds:
      - docker compose down {{.CLI_ARGS}}

  restart:
    cmds:
      - task: down
      - task: up
